{% if oView.isEnabled() %}
    <div id="cookieNote">
        <div class="alert alert-info sk-cookie-note" style="margin: 0;">
            <button id="cookieNoteCloseButton" type="button" class="btn-close" data-bs-dismiss="alert">
                <span aria-hidden="true">Ok</span><span class="visually-hidden-focusable">{{ translate({ ident: 'CLOSE' }) }}</span>
            </button>
            Um unsere Webseite f&uuml;r Sie optimal zu gestalten und fortlaufend verbessern zu k&ouml;nnen, verwenden wir Cookies.
        </div>
    </div>
{% endif %}
{{ script({ widget: oView.getClassKey(), dynamic: __oxid_include_dynamic }) }}
